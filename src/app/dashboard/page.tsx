"use client";

import Button from "@/components/ui/Button";

const Dashboard = () => {
  return (
    <div>
      Dashboard
      <Button type="button">Logout</Button>
    </div>
  );
};

export default Dashboard;
